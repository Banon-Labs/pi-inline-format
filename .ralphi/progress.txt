## Codebase Patterns
- Keep the Pi-facing TypeScript wrapper minimal and move transcript analysis contracts into the Rust crate.
- Document stable Rust CLI JSON contracts in `README.md` when later iterations need to consume them from TypeScript.
- Ignore `.pi/` local Pi state in git so Ralph commits stay focused on product code.
- When separating transcript wrappers from embedded code, keep wrapper text as one or more ordered outer regions instead of leaving the embedded body inside a single bash span.
- When a TypeScript wrapper can reuse `rust/target` binaries, guard that fast path with a freshness check so stale debug binaries do not silently drift from the Rust sources.
- When Pi-facing output needs separate code fences, keep the markdown formatting as a pure projection of Rust `render_blocks` so TypeScript does not re-split transcripts.

## 2026-03-28 21:35 UTC - US-001
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Defined a generic Rust transcript analysis contract with ordered `regions` and explicit `role` metadata.
- Documented the JSON contract in `README.md` and reinforced the generic contract rule in `AGENTS.md`.
- Files changed: `rust/src/lib.rs`, `README.md`, `AGENTS.md`
- **Learnings for future iterations:**
  - `RegionRole` is the stable place to distinguish outer transcript content from embedded code.
  - Byte offsets are a better future-proof contract than Python-specific boolean flags.
---

## 2026-03-28 21:45 UTC - US-002
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Added Python heredoc detection in the Rust core so transcript analysis now returns both outer bash and embedded python regions.
- Kept the contract generic by reusing `regions`, `role`, `language`, and byte offsets rather than adding Python-only response fields.
- Ignored `.pi/` local session artifacts in git to keep future Ralph commits focused on repo code.
- Files changed: `rust/src/lib.rs`, `README.md`, `.gitignore`, `.ralphi/prd.json`
- **Learnings for future iterations:**
  - `find_map` is the cleanest strict-clippy-friendly way to scan multiple heredoc markers without tripping `never_loop`.
  - Detection can arrive before extraction: the current byte offsets are enough to prove nested-language presence before wrapper/content cleanup work in US-003.
---

## 2026-03-28 22:04 UTC - US-003
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Split bash wrapper content into ordered outer regions around the embedded Python heredoc body.
- Added Rust tests that verify embedded extraction excludes heredoc markers and that incomplete heredocs fall back to a single outer bash region.
- Documented the updated contract example in `README.md` so future wrapper work can consume separated wrapper and embedded spans.
- Files changed: `rust/src/lib.rs`, `README.md`, `.ralphi/prd.json`, `.ralphi/progress.txt`
- **Learnings for future iterations:**
  - Wrapper separation is easiest to consume when outer regions keep the heredoc opener and terminator while the embedded region contains only the nested language body.
  - Strict clippy settings favor `Option::map_or_else` over `if let` when building fallback region lists from an optional match.
---

## 2026-03-28 22:14 UTC - US-004
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Added a dedicated `extensions/rust-cli.ts` helper that runs the Rust CLI, validates the JSON contract, and reports command-level failures clearly.
- Kept `extensions/index.ts` Pi-facing by wiring the Rust helper into one slash command, one tool, and a lightweight status command instead of duplicating transcript parsing in TypeScript.
- Added a freshness guard so stale `rust/target` binaries fall back to `cargo run` instead of returning outdated analysis results.
- Documented the wrapper integration in `README.md` and captured the helper pattern in `AGENTS.md`.
- Files changed: `extensions/index.ts`, `extensions/rust-cli.ts`, `README.md`, `AGENTS.md`, `.ralphi/prd.json`, `.ralphi/progress.txt`
- **Learnings for future iterations:**
  - A dedicated Rust CLI helper keeps Pi command/tool registration small while still surfacing invocation details for debugging.
  - Reusing an existing `target/debug` binary is unsafe without a freshness check because `cargo test` and `cargo check` can leave a stale standalone binary behind.
---

## 2026-03-28 22:22 UTC - US-005
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Added generic Rust `render_blocks` alongside `regions` so bash wrapper text and embedded Python code can render as distinct language-aware blocks.
- Added Pi-facing `/inline-format-render` and `render_inline_transcript` entrypoints that project Rust `render_blocks` into markdown code fences without duplicating transcript parsing in TypeScript.
- Expanded the Rust/TypeScript contract docs and shape validation so downstream consumers can rely on the new rendering data.
- Files changed: `rust/src/lib.rs`, `extensions/rust-cli.ts`, `extensions/index.ts`, `README.md`, `AGENTS.md`, `.ralphi/prd.json`, `.ralphi/progress.txt`
- **Learnings for future iterations:**
  - Rendering-friendly transcript output can stay generic by pairing byte-oriented `regions` with extracted `render_blocks` instead of inventing Python-specific fields.
  - TypeScript can stay thin when it only formats the Rust-provided `render_blocks` into Pi-friendly markdown/code fences.
---
